<div id="test">
<% unless @disorders.nil? %>
  <b>Search Results</b>
  <%= puts @disorders.inspect %>
  <%= @disorders.each do |disorder| %>
    <p><%= "hello!" %></p>
  <% end %>
<% end %>
</div>

<%= form_for 'search_disorder_path' do |f| %>
  <div class="field">
    <%= f.label :search %>
    <%= f.text_field :q %>
  </div>
  <div class="actions"><%= submit_tag "Search" %></div>
<% end %>